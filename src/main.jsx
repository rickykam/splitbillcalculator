import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.js' // 確保這裡指向您的主程式檔名
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
